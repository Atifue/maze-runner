Intialize npm with defaults:

npm init -y 

Install mineflayer:

npm install mineflayer

I'm running 1.21.8, but reference the documentation for what this api supports


On minecraft:

Create a world, and open the lan server and replace the port when intiailzing the bot
Run node index.js




currently we are avoiding walking backwards, but we should be learning from each step, 